[project]
name = "generate-brush"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "transformers==4.30.0",
    "huggingface_hub==0.20.2"
]
